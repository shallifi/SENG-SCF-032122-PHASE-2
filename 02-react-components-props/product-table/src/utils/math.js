export const add = (num1, num2) => num1 + num2

export const subtract = (num1, num2) => num1 - num2

// or export { add, subtract } 

// if we use the export statement on line 5, remove `export` from lines 1 & 3 and just leave as standard arrow functions